[package]
name = "jtop"
version = "1.0.0"
edition = "2021"

[dependencies]
crossterm = "0.27"
libc = "0.2"
num_cpus = "1.16"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "unwind"
